# CLAUDE.md

Guidance for working in this repo. Behavioural principles first, then the
project-specific facts, invariants, and workflow. Bias toward caution over
speed; for trivial tasks, use judgment.

## How to work

**1. Think before coding.** State assumptions; if uncertain, ask. Surface
multiple interpretations instead of silently picking one. Say so when a simpler
approach exists. If something's unclear, stop and name it.

**2. Simplicity first.** Minimum code that solves the problem — nothing
speculative. No unrequested features, single-use abstractions, "flexibility,"
or error handling for impossible cases. If 200 lines could be 50, rewrite it.

**3. Surgical changes.** Touch only what the request needs. Don't refactor,
reformat, or "improve" adjacent code; match the existing style even if you'd do
it differently. Remove only the imports/vars YOUR change orphaned; mention
pre-existing dead code rather than deleting it. Every changed line should trace
to the request.

**4. Goal-driven execution.** Turn the task into a verifiable goal and check it:
typecheck (`npx tsc --noEmit`), `npm run build`, and a smoke test of the actual
route. For multi-step work, state a brief plan with a verify step each.

## Project overview

Companion — a Next.js 16 (App Router, Turbopack) + React 19 + TypeScript app,
styled with CSS Modules, deployed on Vercel
(`moonlightdevelopers-projects/companion-app`) with Neon Postgres. The user
pastes or uploads screenshots of a confusing dating conversation and gets a
live, behaviour-based read generated by Claude — then can keep a private,
per-person history of those reads.

`index.html` and `app.html` at the root are the design source of truth —
preserve them; port from them, don't delete them.

**What exists today (shipped, FLAG-1 → FLAG-18):**

- The guided `/story` flow: one-question-at-a-time intake → paste or upload
  screenshots (Claude vision → ordered transcript) → a live behaviour read.
- Email-gated read with an optional emailed copy (Resend) + delete-my-data.
- Passwordless sign-in: magic-link (`/signin`) **and** inline OTP code inside
  the flow (FLAG-13) — the in-flow email step signs you in, so the read attaches
  to your account; already-signed-in users skip the step.
- Signed-in persistence: `users → persons → reports` in Postgres; raw
  conversations on-device only (IndexedDB).
- Pick-or-create entry and reply assistance.
- Per-person history & pattern over time (§2.5, FLAG-14) — calm person
  overview, synthesized pattern insight, finite past-reads list. History is
  free; the cross-report pattern insight is the paid tier (free users get a
  one-shot teaser). Verified end-to-end against the real DB.
- Storage cleanup (FLAG-15): `report.result` is single-encoded jsonb; the read
  path handles exactly one shape (the defensive double-decode parse was removed
  after a one-time migration). Do not reintroduce double-encoding.
- Evidence-vs-identifying-detail scrub (FLAG-16, §2.2): stored reads keep a
  short proving quote but generalize incidental identifying specifics.
- Voice pass (FLAG-17, §2.10): the read, pattern line, and follow-ups speak as
  a sharp, plain-spoken friend that's openly a tool — warm but honest-over-nice.
- Pre-read clarification (FLAG-18, §2.11): before the read, 0–2 skippable
  questions resolve genuine verdict-forking ambiguity in the chat; usually zero.

**Planned (designed, not yet built):**

- Soft identity (§2.8) — recognize a returning user on their own browser
  without forcing an account or a code, so the per-person features reach real
  users without first-read friction. This supersedes FLAG-13's OTP gate.
- Language & cultural-context handling (§2.9) — a prompt/UX policy, mostly
  unbuilt.

**Known gaps (important):**

- First-read friction vs. frictionless-anonymous ideal. FLAG-13 wired sign-in
  into the flow via an inline OTP code, which fixes recognition — but it gates
  the first read behind a code, which sits against §2.5/§2.8's "free anonymous
  first read." §2.8 (soft identity) is the designed fix: keep the first read
  anonymous and code-free, recognize the returning user via a device token, and
  keep magic-link as the optional cross-device upgrade. Until §2.8 lands, the
  OTP gate stands. Do not "solve" recognition by hard-gating the read behind
  auth (that breaks §2.5).
- Email is sandboxed (Resend). Codes/reads only deliver to the Resend account
  owner's address until the sending domain is verified — real-user testing is
  blocked on this DNS step.

## Commands

- `npm run dev` — dev server (http://localhost:3000)
- `npm run build` — production build
- `npm run lint` — ESLint
- `npx tsc --noEmit` — typecheck (there is no dedicated script)

**Gotcha:** after `npm run build`, always `rm -rf .next` before `npm run dev`,
or every route 404s (dev server chokes on a production `.next`).

## Architecture

- `app/page.tsx` — landing (ported from `index.html`)
- `app/story/page.tsx` — the guided flow + screenshot upload/extraction/review,
  pick-or-create entry, per-person overview/history, and reply help (ported from `app.html`)
- `app/signin/page.tsx` — magic-link sign-in; `app/delete/page.tsx` — delete-my-data
- `app/api/analyze` → `lib/analyze.ts` — the behaviour read (Claude → JSON)
- `app/api/extract` → `lib/extract.ts` — screenshots → ordered transcript (Claude vision)
- `app/api/reply` → `lib/reply.ts` — 2–3 reply drafts (Claude)
- `app/api/auth/{request,verify,signout}` → `lib/auth.ts` — magic-link + session
- `app/api/auth/otp` + `app/api/auth/otp/verify` → `lib/auth.ts` + `lib/db.ts` — inline OTP sign-in
- `app/api/me` → `lib/auth.ts` — session check (am I signed in, as whom)
- `app/api/persons` → `lib/db.ts` — the signed-in user's roster
- `app/api/persons/[id]/reports` + `app/api/persons/[id]/pattern` → `lib/db.ts` + `lib/pattern.ts` — per-person history (§2.5)
- `app/api/delete-data` → `lib/db.ts` — remove saved reads by email
- `lib/db.ts` — Postgres (`users/persons/reports/reads/login_codes`); `lib/email.ts` —
  Resend (`sendReadEmail`, `sendMagicLink`, `sendLoginCode`); `lib/localConversations.ts` —
  IndexedDB; `lib/prompt.ts` — read prompt; `lib/pattern.ts` — pattern-insight synthesis
- `types.ts` — shared types (`Read`, `Intake`, `Transcript`, `ReplyDraft`, …)
- `scripts/migrate-double-encoded.mjs` — one-time jsonb normalization (FLAG-15)

Model output is always run through a shape-guard so a missing/stray field
can't crash the UI. Add new fields there, not just in the prompt.

## AI

- Claude via the official `@anthropic-ai/sdk`, server-side only.
- Default model `claude-sonnet-4-6`; override with the `MODEL` env var. Note:
  `MODEL` is currently a single global — all Claude calls (read, extract, reply,
  pattern) use it. Per-path model selection would need a thin wrapper (not built).
- Model selection by path. Sonnet 4.6 is the cost-balanced default. The
  nuance-critical paths (the read, the reply, the pattern insight) benefit from
  a stronger model — set `MODEL` (e.g. `claude-opus-4-8`) if quality warrants.
  Select for instruction-following + low sycophancy, not maximum "empathy": a
  model that's too warm/agreeable will reassure the user instead of giving the
  honest read, which is the exact failure this product exists to avoid. Keep
  model use behind the env var / a thin wrapper so providers are swappable — the
  frontier shifts monthly.
- The app reads behaviour, not feelings or an interest percentage. Keep that
  framing in prompts and copy.

## 2. Data & memory model

The engineering reference behind the "saved people / private history / replies"
work. Shipped: FLAG-8 (magic-link sign-in), FLAG-9 (saved people), FLAG-10
(on-device conversations), FLAG-11 (pick-or-create), FLAG-12 (reply help),
FLAG-13 (inline OTP sign-in), FLAG-14 (per-person history & pattern over time),
FLAG-15 (storage-shape cleanup), FLAG-16 (evidence scrub), FLAG-17 (voice, §2.10), FLAG-18 (pre-read clarification, §2.11). Designed, not yet
built: soft identity (§2.8), language & cultural context (§2.9).

**Identity (decided):** passwordless email magic-link / OTP via Resend. User key
is a UUID `userId`; `email` is a `UNIQUE NOT NULL` attribute, never the primary
key (email is mutable; a key shouldn't be, and a UUID keeps PII in one row).

### §2.1 Entity model & data lifecycle

Postgres, `users → persons → reports`:

- `users(id uuid pk, email unique not null, created_at)`
- `persons(id uuid pk, user_id uuid fk→users on delete cascade, nickname,
  normalized_nickname, differentiator nullable, created_at,
  UNIQUE(user_id, normalized_nickname))`
  - `person.id` is a server-generated UUID. The nickname is a mutable label,
    not the key. Nickname uniqueness is scoped per user via
    `(user_id, normalized_nickname)` — never global.
- `reports(id uuid pk, person_id uuid fk→persons on delete cascade,
  result jsonb, created_at)` — a report belongs to a `personId`.
  - `result` is a single-encoded jsonb object (the read). Never serialize JSON
    into it as a string (that was the FLAG-15 bug — store via postgres.js
    `sql.json`). The read path assumes one shape — don't add a both-shapes
    fallback.
- `login_codes(email pk, code_hash, expires_at, attempts)` — short-lived hashed
  OTP codes, attempt-capped (FLAG-13).

**Raw conversations live ONLY in IndexedDB**, tagged with `personId + reportId`.
There is no conversation table, column, or JSON blob in Postgres — ever.

**Create order (must hold):** generate read → Postgres returns report +
`personId` → IndexedDB stores the conversation.

**Local conversation lifecycle:** max 10 per person (rolling), 30-day TTL, lazy
eviction on app-open and on write.

### §2.2 Reports quote their own evidence — but not identifying detail

A report (the read `result` JSON) is self-contained: it cites the specific
behaviours/lines it read from. So an old report renders as a full read
standalone — no raw conversation needed. Reports are readable from Postgres
forever and cross-device; the conversation is only ever needed for a fresh read
or a reply.

Evidence vs identifying detail (FLAG-16). A stored read may keep a short quoted
phrase that proves the behaviour (e.g. "maybe, I'll let you know"). But it must
strip incidental identifying specifics — named places, neighbourhoods, cities,
venues, employers, schools — and replace them with a neutral generalization
("a specific place", "their work", "a particular day"), including when the
specific is inside a quote. Keep the behaviour and the proving quote; drop the
identifying particular. The other person is only ever the nickname. Enforced at
the prompt level (`lib/prompt.ts`, mirrored in `lib/pattern.ts`); a deterministic
NER/regex scrubber was deliberately rejected as brittle — accept occasional
slippage as a known low-harm residual risk rather than build a fragile filter.
Forward-only: existing rows can't be re-scrubbed (the source conversation is
gone); the only remedy for an old row is deleting that report.

### §2.3 Calm by design (no engagement mechanics)

History is something you read and close, not a feed you refresh. Forbidden:
unread badges, "new insight" pulses, come-back/notification prompts, infinite
scroll. Lists are finite and newest-first. The pattern insight points toward
resolution ("here's what keeps happening to you"), never re-checking ("check
back for changes"). Frame everything as the user's own pattern, never a
profile/dossier of the other person (§2.7); no identifying notes on screen.

### §2.5 Pattern over time (per-person history) — shipped (FLAG-14)

Selecting an existing person opens a calm person overview, not a new read:

- Pattern insight — if ≥2 prior reports, one quiet, plain-language observation
  synthesized from them (they quote their own evidence, §2.2, including the
  identifying-detail scrub), pointing toward clarity. One report → no pattern
  line yet. Must not manufacture a false "you keep getting X" pattern out of
  contradictory reads — reflect inconsistency honestly, and stay
  behaviour-anchored, never intent ("they're stringing you along").
- Actions: New report (primary; skips onboarding/context questions — person
  already chosen) and see past reads (secondary → finite list, newest-first →
  open one = full read standalone).
- Reply on an opened report follows §2.6 (recent few + conversation still in
  IndexedDB; older reports show the read with no reply button — render cleanly
  as an intentional state, it's expected, not an error/empty state).

Reports query (ownership-checked, cross-device, independent of IndexedDB):

```sql
SELECT r.id, r.result, r.created_at
FROM reports r JOIN persons p ON p.id = r.person_id
WHERE r.person_id = $1 AND p.user_id = $2
ORDER BY r.created_at DESC;
```

Pricing (decided): full history is free; the cross-report pattern insight is the
paid upgrade (the existing "Companion" subscription tier — follows the story
over time). Free users see the latest read + a single, calm teaser that a
pattern exists. Keep the teaser one-shot, never a recurring nudge (§2.3). Not
gated yet — confirm before gating.

### §2.6 Reply assistance

Available only when the raw conversation is present right now (live session, or
in IndexedDB on return) and only on the 3 most recent conversations per person.
If it's absent, the option is simply gone — never re-solicit the conversation.
Output 2–3 editable draft tones from the user's stated intent; never persist
drafts.

It is a clarity tool, not a tactics tool: help the user say what they mean (ask
directly, make a plan land, set a boundary), never engineer a reaction (make
them chase, play it cool). Drafts mirror the user's own voice and the
conversation's language/register (§2.9), and move toward resolution.

### §2.7 Pick-or-create entry

On entry, if the user has saved persons, offer "someone new, or one of these?"
The roster comes from a Postgres query on `persons` by `userId` — NOT from
IndexedDB — so it survives a device switch. Pick → attach to that `personId`,
skip setup. New → normalize the name, check against the Postgres roster, and on
collision suggest a non-identifying differentiator (e.g. "Alex – Hinge", never
real PII). Keep the roster lightweight ("your saved stories"), never a dossier
of the other people.

### §2.8 Soft identity (designed — not yet built)

Resolves the first-read-friction gap without forcing an account or walling the
read. Three tiers: anonymous → soft (device-recognized) → verified (magic-link,
cross-device). **This supersedes FLAG-13's OTP first-read gate** — adopting §2.8
means the first read no longer requires a code.

- Anonymous first read stays free and frictionless — no auth, no code. This is
  load-bearing (§2.5); never gate the read behind sign-in.
- On first report completion, mint a soft identity: an opaque token in an
  httpOnly first-party cookie + a soft-user record. Persons/reports attach to it
  exactly as for verified users.
- The token is the recognition key — never the email alone. Email (from the
  existing consent gate) is a stored attribute. Recognition gate = token present
  AND entered email matches the soft-user's stored email. Both must match — this
  stops a different person on the same device, or someone typing a known email
  elsewhere, from seeing the roster.
- Device-scoped by design. New device / cleared storage → looks new again.
  Cross-device recognition is the optional verified (magic-link) upgrade, which
  links the same email across devices. Do not build cross-device into the soft
  tier.
- The token is a persistent identifier → it needs a light consent
  acknowledgment; don't ship it silently. Do not capture/store geographic region
  (IP geolocation) — language detection uses the browser locale only (§2.9).
- Invariants unchanged: raw conversations IndexedDB-only; Postgres holds only
  the soft-user (opaque id + email), nickname-only persons, and scrubbed reports.
- Schema detail to decide at build: whether a soft-user is a `users` row with a
  verified flag (magic-link upgrades it) or a separate table. Plan before
  building.

### §2.9 Language & cultural context (designed — policy)

- Language is auto-detected from the browser locale, never from region/IP, and
  is always user-overridable (one tap) — detection is a default, not a lock.
  Never persist region.
- Read output is in the user's language (UI/locale). Reply drafts are in the
  conversation's language and register (§2.6) — mirror code-switching and slang
  so a draft is actually sendable.
- Culture is a humility input, never a verdict generator. The user may
  optionally supply cultural context; the model uses it only to calibrate
  confidence in both directions — don't over-read directness/low-warmth as a bad
  sign, and don't wave off genuinely concerning behaviour as "just cultural." The
  app must never infer, store, or generate claims from culture/ethnicity (no "X
  people typically…"). The read stays anchored to what this person did; cultural
  context only adjusts how hard it leans on tone.
- Nuance quality degrades in lower-resource languages even when literal
  comprehension holds — prefer calibrated/uncertain reads there (§2.2), and test
  subtext on real non-English cases rather than assuming parity with English.

### §2.10 Voice (shipped — FLAG-17)

The read and its follow-ups speak like a sharp, plain-spoken friend who is
openly a tool — warm, direct, takes a position. Honesty IS the friendliness:
blunt because it's a tool with no stake; never soften a real red flag to be nice
(comforting over true is the failure this product exists to avoid). No performed
feelings, no therapy-speak, no corporate hedging, no folksy/cute quips. Plain
words, contractions, short sentences.

Voice only — it never loosens the analysis: §2.2 (behaviour not intent, cite
evidence, strip identifying detail, no mind-reading) and §2.3 (no
engagement-bait) still hold; warmer wording must not soften the verdict. Enforced
at the prompt level (`lib/prompt.ts`, mirrored in `lib/pattern.ts`) and in the
read/reply UI copy.

Follow-ups always hand the user an action toward the exit (e.g. "want me to draft
what you'd send?", the one-shot pattern teaser), never open-ended emotional
probing ("how do you feel?") that deepens rumination (§2.3).

### §2.11 Pre-read clarification (shipped — FLAG-18)

Before the read, a short pass inspects the conversation and asks **0–2 skippable
questions** — only where a genuine ambiguity would fork the verdict
(`lib/clarify.ts`, `app/api/clarify`). Biased hard toward **zero**: most chats
ask nothing and go straight to the read (the user already answered intake — don't
stack an interview). Hard cap 2, no follow-up tree.

- Questions are about the **conversation's meaning / missing context only** —
  never the user's feelings (§2.3/§2.10). Plain, specific, neutral (never leading
  toward the comforting reading).
- Answers feed the read prompt (`buildUserMessage` "Clarifications" section). A
  **skipped/ambiguous answer is read as unknown — the read says so, never
  guesses** (calibrate, don't declare).
- Clarification answers obey the same identity scrub (§2.2/FLAG-16): an answer
  can't leak a place/employer into the stored read.
- **Never blocks.** Any failure, or skipping everything, still yields a
  calibrated read — clarify is a sharpener, never a gate.

## Invariants (hard rules — don't break these)

- Secrets are server-side only: `ANTHROPIC_API_KEY`, `RESEND_API_KEY`,
  `DATABASE_URL`, `SESSION_SECRET`. Never expose them to the browser.
  `.env.local` is gitignored and must never be committed; `.env.example`
  documents the keys.
- Never store or log the pasted conversation or the screenshots. Only the read
  result (plus email + nickname) is persisted. Extraction images and the
  transcript are processed in memory and discarded.
- Never use a real name from a screenshot — only "You" and the chosen nickname,
  everywhere (transcript, stored read, email). Stored reads also strip incidental
  identifying specifics (§2.2).
- Emails contain the read only, never the conversation; include the
  delete-my-data link.
- Email/DB are non-blocking: a mail or save failure must never hide or break the
  on-screen read.

## Git / PR / Jira workflow

- Branch per ticket, name must contain the Jira key, e.g. `feat/FLAG-6-email-read`.
  Don't commit on main.
- Commit format (enforced by `.githooks/commit-msg`): `type(TICKET): subject`, or
  `type: subject` for genuine quick fixes. Types: feat, fix, chore, docs,
  refactor, test, style, perf, build, ci. See the `commit-format` skill.
- Open PRs with `scripts/open-pr.sh` (see the `open-pr` skill). A repo autolink
  makes `FLAG-N` clickable in commit/PR bodies.
- No Claude boilerplate in commits or PRs: no "Generated with Claude Code" line,
  no Jira footer, and no `Co-Authored-By` trailer. Keep messages plain.
