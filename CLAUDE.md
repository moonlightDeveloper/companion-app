# CLAUDE.md

Guidance for working in this repo. Behavioural principles first, then the
project-specific facts, invariants, and workflow. Bias toward caution over
speed; for trivial tasks, use judgment.

## How to work

**1. Think before coding.** State assumptions; if uncertain, ask. Surface
multiple interpretations instead of silently picking one. Say so when a simpler
approach exists. If something's unclear, stop and name it.

**2. Simplicity first.** Minimum code that solves the problem — nothing
speculative. No unrequested features, single-use abstractions, "flexibility,"
or error handling for impossible cases. If 200 lines could be 50, rewrite it.

**3. Surgical changes.** Touch only what the request needs. Don't refactor,
reformat, or "improve" adjacent code; match the existing style even if you'd do
it differently. Remove only the imports/vars YOUR change orphaned; mention
pre-existing dead code rather than deleting it. Every changed line should trace
to the request.

**4. Goal-driven execution.** Turn the task into a verifiable goal and check it:
typecheck (`npx tsc --noEmit`), `npm run build`, and a smoke test of the actual
route. For multi-step work, state a brief plan with a verify step each.

## Project

Companion — a Next.js 16 (App Router, Turbopack) + React 19 + TypeScript app,
styled with **CSS Modules**. The user pastes or uploads screenshots of a
confusing dating conversation and gets a live, behaviour-based read generated by
Claude. Deployed on Vercel (project `moonlightdevelopers-projects/companion-app`).

`index.html` and `app.html` at the root are the **design source of truth** —
preserve them; port from them, don't delete them.

## Commands

- `npm run dev` — dev server (http://localhost:3000)
- `npm run build` — production build
- `npm run lint` — ESLint
- `npx tsc --noEmit` — typecheck (there is no dedicated script)

**Gotcha:** after `npm run build`, always `rm -rf .next` before `npm run dev`,
or every route 404s (dev server chokes on a production `.next`).

## Architecture

- `app/page.tsx` — landing (ported from `index.html`)
- `app/story/page.tsx` — the guided one-question-at-a-time flow + screenshot
  upload/extraction/review (ported from `app.html`)
- `app/delete/page.tsx` — delete-my-data form
- `app/api/analyze` → `lib/analyze.ts` — the behaviour read (Claude → JSON)
- `app/api/extract` → `lib/extract.ts` — screenshots → ordered transcript (Claude vision)
- `app/api/delete-data` → `lib/db.ts` — remove saved reads by email
- `lib/email.ts` — `sendReadEmail` (Resend); `lib/prompt.ts` — read prompt
- `types.ts` — shared types (`Read`, `Intake`, `Transcript`, …)

Model output is always run through a **shape-guard** so a missing/stray field
can't crash the UI. Add new fields there, not just in the prompt.

## AI

- Claude via the official `@anthropic-ai/sdk`, server-side only.
- Default model `claude-sonnet-4-6`; override with the `MODEL` env var.
- The app reads *behaviour*, not feelings or an interest percentage. Keep that
  framing in prompts and copy.

## 2. Data & memory model

The engineering reference behind the "saved people / private history / replies"
work. User-facing tickets: FLAG-8 (sign-in), FLAG-9 (saved people), FLAG-10
(on-device conversations), FLAG-11 (pick-or-create), FLAG-12 (reply help).

**Identity (decided):** passwordless email **magic-link / OTP via Resend**.
User key is a **UUID `userId`**; `email` is a `UNIQUE NOT NULL` attribute, never
the primary key (email is mutable; a key shouldn't be, and a UUID keeps PII in
one row).

### §2.1 Entity model & data lifecycle

Postgres, `users → persons → reports`:

- `users(id uuid pk, email unique not null, created_at)`
- `persons(id uuid pk, user_id uuid fk→users on delete cascade, nickname,
  normalized_nickname, differentiator nullable, created_at,
  UNIQUE(user_id, normalized_nickname))`
  - `person.id` is a server-generated UUID. The **nickname is a mutable label,
    not the key.** Nickname uniqueness is **scoped per user** via
    `(user_id, normalized_nickname)` — **never global.**
- `reports(id uuid pk, person_id uuid fk→persons on delete cascade,
  result jsonb, created_at)` — a report belongs to a `personId`.

**Raw conversations live ONLY in IndexedDB**, tagged with `personId + reportId`.
There is **no conversation table, column, or JSON blob in Postgres — ever.**

**Create order (must hold):** generate read → Postgres returns `report` +
`personId` → IndexedDB stores the conversation.

**Local conversation lifecycle:** max **10 per person** (rolling), **30-day
TTL**, **lazy eviction on app-open and on write**.

### §2.6 Reply assistance

Available **only** when the raw conversation is present right now (live session,
or in IndexedDB on return) **and only on the 3 most recent conversations per
person.** If it's absent, the option is simply gone — **never re-solicit the
conversation.** Output **2–3 editable draft tones** from the user's stated
intent; **never persist drafts.**

### §2.7 Pick-or-create entry

On entry, if the user has saved persons, offer "someone new, or one of these?"
The roster comes from a **Postgres query on `persons` by `userId`** — NOT from
IndexedDB — so it survives a device switch. Pick → attach to that `personId`,
skip setup. New → normalize the name, check against the Postgres roster, and on
collision suggest a **non-identifying differentiator** (e.g. "Alex – Hinge",
never real PII).

## Invariants (hard rules — don't break these)

- **Secrets are server-side only**: `ANTHROPIC_API_KEY`, `RESEND_API_KEY`,
  `DATABASE_URL`. Never expose them to the browser. `.env.local` is gitignored
  and must never be committed; `.env.example` documents the keys.
- **Never store or log the pasted conversation or the screenshots.** Only the
  read *result* (plus email + nickname) is persisted. Extraction images and the
  transcript are processed in memory and discarded.
- **Never use a real name from a screenshot** — only "You" and the chosen
  nickname, everywhere (transcript, stored read, email).
- **Emails contain the read only**, never the conversation; include the
  delete-my-data link.
- Email/DB are **non-blocking**: a mail or save failure must never hide or break
  the on-screen read.

## Git / PR / Jira workflow

- **Branch per ticket**, name must contain the Jira key, e.g.
  `feat/FLAG-6-email-read`. Don't commit on `main`.
- **Commit format** (enforced by `.githooks/commit-msg`): `type(TICKET): subject`,
  or `type: subject` for genuine quick fixes. Types: feat, fix, chore, docs,
  refactor, test, style, perf, build, ci. See the `commit-format` skill.
- **Open PRs** with `scripts/open-pr.sh` (see the `open-pr` skill). A repo
  autolink makes `FLAG-N` clickable in commit/PR bodies.
- **No Claude boilerplate** in commits or PRs: no "Generated with Claude Code"
  line, no Jira footer, and **no `Co-Authored-By` trailer**. Keep messages plain.
